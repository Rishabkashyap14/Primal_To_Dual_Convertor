#include<bits/stdc++.h>
#include<iostream>
#include<iomanip>

void canon_primal(int *,
                int *,
                string *,
                vector<int> *,
                vector<int> *,
                vector<string> *,
                vector<vector<int>> *,
                vector<string> *,
                int *,
                int *,
                vector<int> *,
                vector<int> *,
                vector<string> *,
                vector<vector<int>> *,
                vector<string> *)
{
